package span

templ Optional() {
    <span class="text-sky-700">{children...}</span>
}

templ Advice() {
    <span class="text-slate-500">{children...}</span>
}

templ Default() {
    <span class="text-lg text-sky-500">{children...}</span>
}